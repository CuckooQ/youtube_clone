extends layouts/main
include mixins/videoBlock

block content
    div.search__header
        h3 Searching for #{searchText}
    div.search__videos
        if videos.length === 0
            h5 No Videos 
        each video in videos
            +videoBlock(video)
